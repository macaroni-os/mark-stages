hooks:
- type: "inner-chroot"
  name: "arm64-setup"
  description: |
    Remove packages from rootfs.
  entrypoint:
    - /bin/bash
    - -c
  commands:
    # compile evdev needs /usr/src/linux
    - >-
      eval 'mkdir -p /usr/src || true' &&
      USE="symlink" emerge vanilla-sources -j

